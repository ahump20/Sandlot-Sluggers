{
  "name": "blazesportsintel",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace web && npm run deploy --workspace api",
    "dev": "npm run dev --workspace web",
    "lint": "npm run lint --workspace web",
    "typecheck": "npm run typecheck --workspace web && npm run typecheck --workspace api"
  }
}
